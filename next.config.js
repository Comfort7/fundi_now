/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  images: {
    domains: [],
  },
  trailingSlash: false,
}

module.exports = nextConfig

module.exports = nextConfig
